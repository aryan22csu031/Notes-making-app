<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="text-6xl text-center font-semibold bg-zinc-900 text-yellow-600 py-9">MY NOTES</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input type="text" placeholder="Title goes here" class="block px-5 py-2 bg-zinc-800 w-full rounded-lg" name="title">
                <textarea placeholder="Write your task here" class="block px-5 py-2 mt-2 bg-zinc-800 resize-none w-full rounded-lg" name="details"></textarea>
                <input class="mt-2 bg-green-800 cursor-pointer rounded-md p-2 text-white hover:bg-green-900" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-5 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach((val) => { %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"> <%= val %> </h1>
                        <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-3">Read more...</a>
                        <a href="/edit/<%= val %>" class="bg-yellow-600 p-2 rounded-md relative left-[3.8rem] text-black font-bold hover:bg-yellow-700">Edit Filename</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-600">No tasks yet...</h3>
            <% } %>
        </div>
    </div>
</body>
</html>